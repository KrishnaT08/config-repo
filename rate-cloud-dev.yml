eureka:
    instance:
        appname: rate
        hostname: ${vcap.application.uris[0]}
        nonSecurePort: 80        
    client:
        fetch-registry: true
        register-with-eureka: true       
        
